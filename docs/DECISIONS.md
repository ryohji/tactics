# 決定ログ

## 2026-06-27: 第0回 — MVP と土台方針

### MVP の狙い
- 選択: 「3D地形 × 3Dヘックスの合成プロトタイプ」。移動・表示の核は仕様書で見えているため、次は地形との合成の手触りを検証する。
- 理由: 体験の核が「地形分割・移動・表示が気持ちよくハマる」ことだから、合成が成立するかを最初に確かめるのが最短。
- 可読性A/B(仕様13章)は同時に少しだけ触れるが、本格検証は次イテレーション。

### 地形の表現形式
- 選択: **真の3Dボリューム**(オーバーハング・洞窟を含みうる)。
- 理由: 「3Dをヘックスで分割する」面白さを最大化でき、体験の核と一致する。
- 却下/保留した案: 高さマップ(オーバーハング不可で3Dの旨味が減る→却下)、実験用の固定形状手置き(却下はせず、ボリュームの初期サンプル形状として利用してよい)。

### 地形とセルの重なり判定
- 選択: **セル中心から距離 d 以内に地形があれば進入禁止 + 遮蔽**。
- 理由: ユーザー指定。`d` を調整軸として持て、手触りのチューニングがしやすい。
- 保留した案: 胞(菱形十二面体)の体積占有率(より厳密、後で厳密化候補)、中心の地形内外二値(粗い)。

### 技術前提
- 選択: Web 版(Three.js / React Three Fiber 系)先行。
- 理由: 足場を早く固める。展開先を見てからエンジン採用を検討する。

### 設計フェーズに持ち越す論点(テックリードへ)
- 真の3Dボリュームの内部表現(SDF / ボクセル / メッシュ)と、「中心から距離 d 以内に地形」を効率よく判定する方法。
- 地形の見せ方(ボリューム表示 vs 占有セルの菱形十二面体塗り)。

## 2026-06-27: 設計フェーズ — 地形表現の確定

### 地形の内部表現と距離d判定
- 選択: **SDF(符号付き距離関数)**。判定は `sdf(セル中心) ≤ d` の1式。
- 理由: dの実時間調整が定数1個の変更で済む / オーバーハング・洞窟の1形状を解析合成で容易に作れる / 描画も同じSDFを Marching Cubes に通すだけで、分類と表現が同一ソースから出て不整合が原理的に起きない / it-1 のセル数(数千)では再計算は active・パラメータ変更時のみで最適化不要。
- 却下/保留: ボクセル(分類と描画のソースが分離し不整合リスク)、メッシュ(距離d判定にBVH最近接が要り it-1 には重い)。
- 将来差し替え: `Terrain` インタフェース(`sdf(p)` / `mesh()`)の裏で Voxel/Mesh に置換可能な構造を DESIGN.md に確保済み。

### 採用スタック(テックリード推奨を承認)
- Vite + TypeScript + React Three Fiber(+ drei)、状態管理 zustand、パラメータUI leva。
- 全ロジックは格子座標空間。`worldPos`(S込み)は描画専用に閉じる。マーカーは InstancedMesh。

## 2026-06-28: it-1 QA フィードバック → it-2 方針

### 用語確定
- 「占有セル」= 進入禁止+遮蔽セル(`occluderSet`、セル中心から距離 d 以内に地形)。

### バグ(it-2 で修正)
- B1 表示トグル切替で再描画が効かない(地形トグルが leva ローカルで非リアクティブ)。→ 表示トグルを `store.toggles` に昇格し単一の真実に。
- B2 視点ドラッグ回転でセルが消える(InstancedMesh のフラスタムカリング/バウンディング未更新)。→ `frustumCulled=false` か bounding 更新で対処。

### 地形の作り直し(it-2)
- T1 現サンプルは平坦・狭く、下半分が平らな地面で全部進入不可になり検証にならない。→ アリーナを縦横に貫き、通行可能空間と進入禁止の塊が両方はっきり読める形へ。高低差を持たせ層をまたいだ進入可否が見えるようにする。床で下半分を埋めない。

### 表示の決定(ユーザ確定)
- 自機・移動可能領域(将来の脅威圏も): **稜線とセルをトグル併用**。仕様13章の可読性A/Bにそのまま乗る。
- 地形の見せ方: **ボリュームメッシュと占有セルを併置トグル・既定は占有セル on / メッシュ off**。
- トグル整理(store.toggles): `showTerrainMesh`(false) / `showOccluderCells`(true) / `showRegionCells`(true) / `showRegionEdges`(false) / `showThreat`(false) / `firstPerson`(false)。`showAdjEdges`/`showFog` は廃止(regionEdges に統合)。

### 機能(it-2)
- F1 移動を瞬間移動→時分割アニメ(移動方向へ補間。マーカー+カメラ target を tween)。
- F2 自機+移動可能領域の稜線/セル表示。脅威圏検証のため**敵を1体仮置き**し、脅威セル/稜線も表示。稜線は菱形十二面体エッジ。

## 2026-06-28: it-2 目視フィードバック → 地形アプローチ転換(it-3)

### it-2 目視結果
- 解消: カリング(B2) / 移動アニメ(F1) / 領域表示(F2) / d スライダ。
- 未解消: **再描画バグ**(占有/領域セルのトグル on 初回に最下層1セルだけ描画、移動で復帰)= InstancedMesh の needsUpdate/初期マウント不具合。→ it-3 で修正。
- 地形は「ある程度改善も不十分」、北極星は soso。→ 地形生成のアプローチを転換。

### 地形アプローチ転換(it-3)
- 方針: **崩落エントランスルーム**(ダンジョン入口部屋。壁・床・崩れた天井で閉じた部屋に、折れた柱・落ちた天井塊・瓦礫)を **SDF で自作**。具体シーンにすることで occluder の必然性と北極星の手触りを上げる。
- 作り方: SDF自作で進め、見栄え向上段で外部アセット(CC0崩落ダンジョン系)を再検討。アセットは mesh→SDF/ボクセルベイク or mesh+BVH 判定への切替が要るため、一貫性を保つ実験段では自作を採る。
- 囲い: 壁・天井で閉じた部屋。崩れた天井の抜け穴で上層が見え、折れ柱/崩落塊でオーバーハングと高低差を出す。
- 不変条件: 距離d判定・分類・描画は同一SDFソースで一貫(変更なし)。低層は室内床として過半フリー、occluder は壁・柱・瓦礫・天井で複数層に分布。

## 2026-06-29: 構想転換 — 廃墟大聖堂 / 壮大スケール / 飛行(it-4)

### 世界観
- 地形 = **廃墟化した大聖堂**(壁が一部崩れ内部露出)。崩落エントランスルームより大幅に大きい。
- ユニット = 人間サイズ ≈ 1セル。飛行できる翼人・魔女のファンタジー設定。

### スケール
- 選択: **壮大**(身長数十倍の大空間)。セル数は数万規模になりうる。
- 理由: 大聖堂の壮大さと飛行の立体機動を出すため。コスト承知(d 再計算・描画に最適化が要る可能性)。

### 移動モデル
- 選択: **全ユニット飛行**。室内外の3D空間すべてが移動可能、地形=実体occluderのみ。
- 実装含意: 現状の `reachable`(近傍 ∩ arena − occluder − 占有)が既に飛行モデル(床支持を要求しない)。**移動ロジックの変更は不要**。
- 副次効果: 床歩行前提が消え、it-3 の「瓦礫を低層 occluder にできない(過半フリーと衝突)」問題は解消。瓦礫・柱・崩落塊は素直に occluder。

### 描画戦略の転換(壮大スケール対応)
- it-1 流「全空きセルを小球で描く」は数万セルで破綻 → **廃止**。
- 代わりに: 地形 occluder(大聖堂シェル)+ active の到達セル(クリック対象)+ 脅威 + ユニット を描く。空きセル全部は出さない(必要なら active 近傍だけ薄く)。
- クリックのレイキャストは pointermove 毎でなく **click 時のみ**に。d 変更は**スロットル**して再計算頻度を抑える。

## 2026-06-30: 地形オーサリング層の導入(it-5)

### 詰まりの診断(オフラインで実描画を確認して確定)
- it-4 の SDF 大聖堂は実際には**閉じたかまぼこ型の塊(Quonset hut)**を出していた。原因: ①完全に閉じた箱で内部が見えない ②樽ヴォールトが建物幅と同径で横長＝垂直性なし ③開口が丸穴3つだけで「半壊」が読めない。「それらしくない」のは幾何が大聖堂の特徴を一つも持たないため。
- 加えて**慢性ブロッカー**: ヘッドレスで `/browse` が WebGL を撮れず、毎イテレーション QA が「ユーザ目視待ち」で停滞。地形を作者以外が見られない＝盲目チューニング。

### 方針(ユーザ確定)
- **地形ブループリント層を新設**(SDF を磨くだけ／CC0メッシュ導入は却下・保留)。40個の生定数を手で彫る作り方そのものを変える。
- 最優先の「それらしさ」= **半壊の劇的さ・開放性**(外から侵入できる廃墟としての立体機動の土台)。輪郭の認識性より先に、大破断・開放・瓦礫を効かせる。

### 実装(不変条件: 分類・描画は同一 sdf の単一ソース。維持)
- `terrain.ts` を **SDFツールキット**(プリミティブ/合成/フレーム変換/Marching Cubes/型)に再編し export 化。具体地形は持たない。
- `cathedral.ts` を新設。`CathedralSpec`(寸法パラメータ + 廃墟部品リスト: breaches/rubble/fallen/brokenPillars/roofGaps)から `buildCathedral(spec): Terrain` を生成。柱位置・アーチ寸法はパラメータから**導出**。breaches/rubble は seed つきで再現可能。既定 `RUINED_CATHEDRAL`、入口 `createSampleTerrain()`。
- バシリカ断面(高く細い身廊＋低い側廊＋クリアストーリー)で垂直性、列柱アーケード(穴あき壁)で見通し、南北側廊の大崩落・身廊中央の屋根抜け・崩れたアーケードベイ・瓦礫の山・倒れ柱で開放性を出す。側廊上部の屋根は撤去し谷状に開放。
- **オフライン地形プレビュー** `npm run preview:terrain`(`scripts/preview-terrain.ts`)を常設。Node で `mesh()` を焼き自前ソフトレンダラで PNG 化(`docs/qa/preview/`)。ブラウザ不要で反復確認でき、慢性ブロッカーを解消。

### 保留した案
- CC0 大聖堂メッシュ導入(mesh+BVH か SDF ベイクへの切替が要り、既存の単一ソース設計を巻き戻す)。実験が安定したら見栄え向上段で再検討(it-3 の判断を踏襲)。

### 検証
- typecheck / 61テスト(terrain ツールキット + cathedral ブループリント幾何 + 既存)/ build すべて緑。
- 実描画はオフラインプレビューで確認: かまぼこ → 「廃墟大聖堂」へ転換を確認。WebGL 上の最終目視はユーザ確認待ち。

## 2026-06-30: スケール見直し + 巨大プリセット(it-5 続き)

### 指摘(ユーザ)
- 1セル=人型1体で見るとサイズ感が合わない。既定大聖堂は身廊高 ~12セル(=人間の約12倍)で、VISION の「身長数十倍の壮大スケール」に届かず「大きめの礼拝堂」止まり。アーチ開口も人間1体がやっと通る幅で窮屈。

### 座標とアリーナの対応(確定)
- `horizRadius = hypot(a, b)`(フレーム水平2軸)、`layer L = u·√3/2`。アリーナは **水平半径 Hmax の円柱 × u∈[2Lmin/√3, 2Lmax/√3]**。身廊長手 a=±naveHalfLen をくるむため Hmax≈naveHalfLen+余白が要る。
- `recommendedArena(spec)` を新設し、地形フットプリント＋飛行余白から Hmax/Lmax を導出。store の既定はプリセットの推奨アリーナから引く(手書き定数を廃止)。

### スケール改訂
- 既定 `RUINED_CATHEDRAL` を人間スケールへ拡大: 身廊長 32 / 全幅 16 / ヴォールト頂 ~20セル(=身長の約20倍)。アーチも数セル幅で人型が悠々通る。推奨アリーナ Hmax27 / L0..21 ≈ **29k セル**。
- d はプリセットに紐付け(壁厚に応じ 0.55)。Rthreat も空間拡大に合わせ 3 へ。

### 巨大プリセット(システィーナ型)
- `aisles` フラグを `CathedralSpec` に追加。`false` で **側廊を持たない単一大空間**(背の高い窓が並ぶ一室の大ホール)を生成。
- `SISTINE_CATHEDRAL`: 身廊長 48 / 幅 12 / ヴォールト頂 ~34セル(=身長の約34倍)。天井が複数箇所抜け、壁が大きく崩れ、巨大な瓦礫が積もる。推奨アリーナ Hmax37 / L0..33 ≈ **85k セル**。
- `PRESETS`(ruined / sistine)+ `DEFAULT_PRESET` を登録。store / preview / 将来の UI から参照。
- `npm run preview:terrain [presetKey]` で任意プリセットを描画(巨大は res 上げ)。出力は `docs/qa/preview/<key>/`。

### 検証
- typecheck / 63テスト / build すべて緑。両プリセットをオフライン描画で確認(廃墟=拡大後も良好、システィーナ=側廊なしの縦長大空間)。WebGL 最終目視はユーザ確認待ち。

## 2026-07-01: 移動が重い回帰の修正(it-5 続き)

### 症状(ユーザ)
- 移動球クリック後に一呼吸おいて位置がジャンプ。ドラッグ回転は問題なし。スケール拡大後に顕在化。

### 原因
- `setActive`(移動)が `refresh → derive` を呼び、**毎回 `computeOccluders` で全アリーナ(29k)セルの sdf 判定を再計算**していた。occluderSet/threatSet は active 非依存(arena・terrain・d・enemies のみに依存)で、移動で変わるのは reachableSet(近傍~12セル)だけ。旧2735セルでは軽かったが10倍で秒オーダのブロックになり、`set({active})`→補間開始の直後に同期ブロックが入り、アニメ前半が飛んで「一呼吸→ジャンプ」に見えていた。
- さらに sdf 自体が重かった: breach/rubble のチャンク位置を**毎評価で rng＋三角関数**(`rotateAxisAngle`)生成していた。

### 修正
1. `setActive` は **reachableSet だけ再計算**(`set({active, reachableSet})`)。occluder/threat は据え置き(参照不変)。→ 移動コスト 約6.3s → 0.02ms。
2. cathedral.ts: breach/rubble のチャンクは seed で決定的なので **spec ごとに1度だけ前計算**し、field は前計算済み球/箱への距離のみ評価(rng/trig をホットループから排除)。屋根スタブの `tiltedBasis` も spec スコープへ。
3. **遠方セルの早期棄却**: field 冒頭で bounds 外の AABB 距離 >1.5 なら即 return(真の sdf もそれ以上で確実に空気)。アリーナは大聖堂より遥かに広い円柱なので大半の外気セルがここで抜ける。MC は bounds 内を sampling するので等値面に影響なし。
- 効果(vite-node 実測): init/d変更の occluder 判定 6284ms → 873ms(約7倍)。ブラウザ warm JIT では更に速い。occluders 数・mesh 形状は不変。

### 検証
- typecheck / 64テスト(移動で occluder/threat を再計算しない参照不変ガードを追加) / build 緑。再描画で見た目不変を確認。

## 2026-07-01: アリーナ切替を UI に露出(it-5 続き)

- `store` に `presetKey` 状態と `setPreset(key)` を追加。地形(`buildCathedral(spec)`)・アリーナ(`buildArena(preset.arena)`)・`d` を一括差し替え、occluder → active 選出 → 敵仮置き → threat / reachable の全導出を作り直す。S/Rthreat は現状維持。同一キー・未知キーは no-op。
- `Controls`(leva)最上部に「アリーナ」ドロップダウン(`PRESETS` から生成: 廃墟大聖堂／システィーナ型)。切替後、変わった d/Hmax/Lmin/Lmax を leva 表示に同期(同期中は各 param onChange の store 反映を `syncing` フラグで抑止し二重処理を防止)。プリセットの Hmax(27/37)・Lmax(21/33)に合わせスライダ上限を 40 へ拡張。
- 検証: typecheck / 66テスト(setPreset の切替・no-op ガード追加) / build 緑。dev サーバ起動エラーなし・UI モジュール配信 200。WebGL 上の切替体感はユーザ確認待ち。

## 2026-07-02: it-6 — ゲームルール層(遊べる戦略ゲームへ)

### ユーザ確定(AskUserQuestion)
- ターン構造 = **陣営交互・全ユニット(FE式)**。戦闘 = **命中率あり**。勝利条件 = **リーダー撃破**。対戦形式 = **シンプルな敵AI**。

### ルール確定(テックリード設計・数値は初版バランス)
- **移動**: ユニットごとの Move 値で BFS(12近傍・等コスト)。通行 = arena 内・非 occluder・非占有。
  - **飛行**(魔女・有翼人・ガーゴイル・死霊王): 全通行セルを移動可。
  - **歩行**(騎士・弓兵・僧侶・骸骨兵・邪術師): 各ステップが**足場セル**(下3近傍のいずれかが occluder、または層=Lmin)に限る。
  - **浮遊付与**: 僧侶・魔女(・邪術師)のスキル。射程2の歩行味方に付与し、次の自陣営ターン終了まで飛行扱い。切れた時に空中なら直下の足場へ降着(ダメージなし)。
- **ZOC**: 各ユニットは自分の12近傍(通行セル)に ZOC を張る。**敵 ZOC セルに入った時点で移動終了**(進入は可・通過は不可)。飛行にも適用 → 空中に「壁」が立つのが 3D の見せ場。
- **戦闘**: 命中% = clamp(攻撃側hit − 防御側evade + 高低差 ±10(攻撃側が上/下層) + 支援(攻撃側隣接味方×8, 最大24) − 被支援(防御側隣接味方×8, 最大24) − 遮蔽25(射撃/魔法で射線が地形をかすめる時), 5..100)。
  - ダメージ = max(1, atk − def + 高低差±1)。命中判定は seed 付き LCG(再現可能)。
  - 射線: 線分サンプルの min sdf ≤ 0.25 → **射線なし**(選択不可)/ ≤ 0.9 → **遮蔽**(−25%)。近接は射線不要。
  - **反撃**: 防御側が生存かつ射程内なら1回反撃(反撃への再反撃なし)。弓は minRange 2 で隣接反撃不可。
- **編成**: 6 vs 6。青=聖翼隊(大魔女=リーダー・魔女・有翼人・聖堂騎士・弓兵・僧侶)/ 赤=亡者の軍勢(死霊王=リーダー・邪術師・ガーゴイル×2・骸骨兵×2)。
- **配置フェーズ**: 開戦前に自軍アンカー周辺の配置ゾーン(通行セル・半径4)内でユニットを置き直せる(自動初期配置あり)。「出撃」で開戦。
- **敵AI**: 攻撃候補(移動先×対象)を 期待ダメ+撃破/リーダー加点−反撃リスク でスコアリングし最良を実行。攻撃不能なら最寄りプレイヤーへ接近。リーダーはプレイヤーが距離8以内に来るまで動かない。
- **見た目/音**: クラス別プリミティブ合成メッシュ + HP バー + 選択リング + 投射体/ヒット演出 + ダメージ数字。HUD(ターンバナー・ユニットパネル・行動メニュー・戦闘予測・ログ・勝敗)。効果音は WebAudio 合成(外部アセットなし)。
- **実装形態**: ゲーム状態は新設 `src/state/game.ts`(zustand)。既存 store はプロトタイプ用 active/enemies/reachable/threat を撤去し、地形・アリーナ・パラメータ・表示トグルに専念。プロトタイプの Markers/Region は撤去し、ゲーム用の Units/TargetField/Effects に置換。

### 保留した案
- 命中率なし(決定的戦闘)・FFT式個別ターン・拠点確保勝利 — ユーザ選択で不採用。
- 落下ダメージ・高低差による射程補正・向き(側面/背面)判定 — 次イテレーション候補。

## 2026-07-02: it-6 目視フィードバック対応(第1回)

### 指摘(ユーザ)
1. 飛行の3D移動範囲(胞塗りの塊)は手前のセルが奥を覆い隠し、範囲内の任意セルを選べない。
2. 配置ターンで敵配置が読めない。ユニット中心の拡縮回転だけでは視点が足りない。索敵は将来として、まずゲーム中いつでも視点を動かすモードと視点リセットが欲しい。

### 対応
- **移動/配置ハイライトをセル中心の小球マーカーに置換**(胞塗り廃止)。小球なら隙間から奥のセルが見えてクリックできる。攻撃/スキル対象は胞の稜線アウトライン(クリックはユニットメッシュが受ける)。敵ZOC警告(琥珀)・明滅は維持。
- **視点モード(freeCam)**: HUD の「🎥 視点モード」でいつでも出入り。モード中は旋回中心がユニット追従を離れ、左ドラッグ=パン(画面平行移動)/右ドラッグ=回転/ホイール=ズーム。配置ターンの敵陣確認にも使う。「⌖ 視点リセット」で角度・距離・追従を既定へ戻す。カメラ角度/距離/自由中心は `state/view.ts` のシングルトンに集約(HUD と CameraRig が共有)。パンのワールド→格子変換は worldPos が S·直交変換であることを利用し `fromFrame(world/S)` の1式。
- 保留: 索敵(視界/霧)はゲームルールとして将来イテレーションで検討。

## 2026-07-03: it-6 目視フィードバック対応(第2回)

### 指摘(ユーザ)
1. ユニット配置を読みやすくするため、グラウンドレベルにヘックスタイルを敷いてほしい。
2. 移動マーカー(球)をフォーカスしたとき、そのレベルの移動可能領域にも同じヘックスタイルをオーバーレイしてほしい。
3. ゲームらしさの演出として SE を追加してほしい。

### 対応
- **ヘックスタイル基盤** `render/hex.ts`: FCC 胞の水平断面六角形(仕様12章 HEX_VERTICES)をタイル化。塗り(triangle fan)と輪郭線を worldPos の線形性でインスタンス配置。タイルはセル中心から -0.42S 沈めて「足元の床」に見せる。
- **地上タイル** `render/HexFloor.tsx`: 「グラウンド」= 足場つき通行セルのうち**直下の鉛直線が底まで地形で連続している**もの(大地・床・瓦礫の山・地続きの壁の棚)。ヴォールト屋根など浮いた構造の上面は除外(鉛直サンプリングで空隙検出)。既定プリセットで約1,600セル・判定 ~1s は盤面変更時のみ(メモ化キーは Set/terrain の実体参照)。
- **レベル別オーバーレイ**: 移動/配置マーカーのホバーで `hoverMarker` を game に記録し、**同じ層 L のハイライトセルへヘックスタイル**(塗り+明輪郭)を重ねる。「水平はヘックス、高さは層で読む」の直観を移動範囲の奥行き読みに適用。ZOC 警告色(琥珀)もタイルに反映。
- **SE 追加(5種)**: cursor(マーカー/対象ホバー)・cancel(戻る)・place(配置確定)・land(降着)・battle(開戦の角笛)。既存13種と合わせ WebAudio 合成のまま。
